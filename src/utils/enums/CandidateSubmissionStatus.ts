export enum CandidateSubmissionStatus {
    SUBMITTED = 'submitted',
    BOOKING_PENDING = 'booking_pending',
    BOOKED = 'booked',
    CONTACTED = 'contacted',
    QUALIFIED = 'qualified',
    NOT_QUALIFIED = 'not_qualified',
    CONVERTED = 'converted',
}